install.packages("tidyverse", dep = TRUE) # comando Para instalar paquetes externos. dependencias True instala todas las dependencias.

library(tidyverse) # Cargar la libreria del paquete, se puede activar tambien desde la pestaña packages

install.packages("magic", dep = TRUE)

library(magic)

magic(6) # comando del paquete Magic

installed.packages() # ver que librerias estan isntaladas

# Cuando cierra s sesion se descargan algunas librerias
# Se pueden cargar librerias a traves de install en la pestaña de packages
